<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.ram.rideindia">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserEntry.UserLoginActivity"
            android:label="User Login"></activity>
        <activity
            android:name=".UserEntry.RegisterActivity"
            android:label="User Register"></activity>
        <activity
            android:name=".admin.admin_home_activity"
            android:label="Admin Dashboard"></activity>
        <activity
            android:name=".UserEntry.UserVerification"
            android:label="Mail Verification"></activity>
        <activity
            android:name=".admin.addNewStudent"
            android:label="Add new Student"></activity>
        <activity
            android:name=".admin.UploadNotice"
            android:label="Upload Important Notice"></activity>
    </application>

</manifest>